πραγματικές: A[R-L+1]
ακέραιες: L, R, mid

Αλγόριθμος MergeSort (A, L, R)
	
	Αν L < R 
		mid <- (l + R) / 2
		MergeSort(A, L, mid) // Αναδρομική ταξινόμηση πρώτου μισού
		MergeSort(A, mid + 1, R) // Αναδρομική ταξινόμηση δεύτερου μισού
		Κάλεσε Merge(A, L, mid, R) // Συγχώνευση των ταξινομημένων μισών
	τέλος_αν
τέλος_αλγορίθμου

πραγματικές: A[], left[n1], right[n2], 
ακέραιες: L, R, mid, n1, n2, i, j, k

Συνάρτηση Merge (A, L, mid, R)

	n1 <- mid - L + 1
	n2 <- R - mid 
	
	Για i από 0 μέχρι n1 - 1
		left[i] <- A[L + i]
	τέλος_επανάληψης
	
	Για j από 0 μέχρι n2 - 1
		right[j] <- A[mid + 1 + j]
	τέλος_επανάληψης

	i <- 0
	j <- 0
	k <- L

	Όσο i < n1 ΚΑΙ j < n2
		Αν left[i] <= right[j]
			A[k] <- left[i]
			i <- i + 1
		αλλιώς
			A[k] <- right[j]
			j <- j + 1
		τέλος_αν
		k <- k + 1
	τέλος_επανάληψης
	
	Όσο i < n1 
		A[k] <- left[i]
		i <- i + 1
		k <- k + 1
	τέλος_επανάληψης
	
	Όσο j < n2 
		A[k] <- right[j]
		j <- j + 1
		k <- k + 1
	τέλος_επανάληψης
τέλος_συνάρτησης